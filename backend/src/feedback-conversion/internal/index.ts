/**
 * WORK-068 — the internal composition surface (the sanctioned wiring
 * boundary for the composition root in src/app.ts).
 */
export {
  DefaultFeedbackConversionService,
  FEEDBACK_CONVERSION_VERSION,
} from './feedback-conversion-service.js';
