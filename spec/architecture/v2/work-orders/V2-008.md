# V2-008 — Computer-Agent Runtime

**Status:** planned · **Depends:** V2-004, V2-005, V2-007

## Objective
Provide the computer-use runtime that maps executable workflow plans to real capabilities on desktop, web and mobile hosts without changing workflow semantics.

## Must deliver
- observation/action loop bounded by WorkflowIR and policy;
- capability invocation through the protocol only;
- deterministic state/evidence recording;
- pause/resume and human takeover;
- failure classification and recovery without inventing successful side effects;
- host adapters for web, desktop and mobile with one protocol contract;
- explicit safe-action boundaries for sensitive capabilities.

## Required regressions
Action authorization, stale observation, wrong-target prevention, duplicate action suppression where required, pause/takeover, recovery, evidence truthfulness and cross-host protocol conformance.

## Dogfooding
Automate one useful computer task end-to-end on a real host, including one intentionally recoverable failure and a human takeover path.
