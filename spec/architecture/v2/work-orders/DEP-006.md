# DEP-006 — Railway API and Worker Deployment

**Status:** PLANNED
**Program:** V2 productionization
**Parallelism:** SERIALIZED RUNTIME FOUNDATION
**Dependencies:** DEP-003, DEP-004, DEP-005

## Objective
Deploy the existing backend modular monolith as API and Worker process roles using the production configuration and authoritative infrastructure.

## Scope
Container/runtime configuration, API role, Worker role, health/readiness, startup ordering, graceful shutdown, migrations ownership, environment wiring, and staging validation.

## Non-goals
Microservice decomposition, workflow/execution authority changes, or provider-specific semantic ownership.

## Required evidence
Staging API + Worker live; authoritative DB/Redis/object-storage integrations verified; readiness fails closed for missing required dependencies; migration race prevention proven; representative V2 route smoke; exact-head review.

## Downstream
DEP-007 may proceed in parallel for frontend delivery after its dependencies; DEP-008, DEP-009 and DEP-010 may consume the deployed API as needed; DEP-011 depends on this slice.

## Completion
Architect merge after staging deployment proof and exact-head verification.
